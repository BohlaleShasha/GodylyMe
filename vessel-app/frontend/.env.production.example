# Production environment variables for Vercel deployment
# Copy this to .env.production for production builds

# Leave VITE_API_BASE_URL empty to use the same origin as the frontend
# This allows the frontend to call /api/* routes on the same domain
VITE_API_BASE_URL=

# Set to false to disable seed data in production
VITE_USE_SEEDS=false
